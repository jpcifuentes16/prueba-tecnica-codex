# Prueba técnica Codex

El proyecto principal se encuentra en `ApiOpenAI/` e implementa una API mínima en .NET 8 con FastEndpoints que consume el cliente oficial de OpenAI (Responses). Revisa el `README.md` dentro de esa carpeta para instrucciones de ejecución y uso.
